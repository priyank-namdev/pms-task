/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // output: 'export', // if you are exporting a static build
  // any other config...
};

export default nextConfig;
